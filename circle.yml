machine:
  php:
    version: 7.0.4

dependencies:
  pre:
    - sudo apt-get install python-dev

deployment:
  production:
    branch: master
    commands:
      - bash ./setup-eb.sh
      - eb deploy strive-main-production

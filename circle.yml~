machine:
  php:
    version: 7.0.4

dependencies:
  pre:
    - sudo apt-get install python-dev
    - pip install awsebcli

deployment:
  production:
    branch: master
    commands:
      - bash ./setup-eb.sh
      - eb deploy

test:
  override:
    